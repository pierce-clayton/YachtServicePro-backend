<%= form_with(model: customer_yacht) do |form| %>
  <% if customer_yacht.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(customer_yacht.errors.count, "error") %> prohibited this customer_yacht from being saved:</h2>

      <ul>
        <% customer_yacht.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :customer_id %>
    <%= form.text_field :customer_id %>
  </div>

  <div class="field">
    <%= form.label :yacht_id %>
    <%= form.text_field :yacht_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
