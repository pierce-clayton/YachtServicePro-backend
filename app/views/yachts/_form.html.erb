<%= form_with(model: yacht) do |form| %>
  <% if yacht.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(yacht.errors.count, "error") %> prohibited this yacht from being saved:</h2>

      <ul>
        <% yacht.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :length %>
    <%= form.number_field :length %>
  </div>

  <div class="field">
    <%= form.label :sail %>
    <%= form.check_box :sail %>
  </div>

  <div class="field">
    <%= form.label :registration_number %>
    <%= form.text_field :registration_number %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
